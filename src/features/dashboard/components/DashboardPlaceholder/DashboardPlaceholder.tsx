import { StyledDashboardPlaceholder } from './styles';

const DashboardPlaceholder = () => (
  <StyledDashboardPlaceholder>
    Sorry, no results.
    <span>Try to type something, or change your query.</span>
  </StyledDashboardPlaceholder>
);

export default DashboardPlaceholder;
